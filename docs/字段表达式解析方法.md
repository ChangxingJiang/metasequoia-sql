# 字段表达式解析方法

我们将用于生成一个字段的 SQL 表达式称为字段表达式（在代码中使用 column expression 表示）。

字段表达式有如下类型：

- 计算字段
- 函数或聚集函数

字段表达式有如下使用场景：

- SELECT 子句中描述字段的语法
- WHERE 子句中的条件表达式的一部分
- GROUP BY 子句中描述字段的语法
- ORDER BY 子句中描述字段的语法
